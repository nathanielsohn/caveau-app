node_modules/
.next/
out/
.env
.env.local
.env*.local
.env.production
logs/
*.log
*.tsbuildinfo
.build-stop
.DS_Store
BUILD_STATUS.json.bak
*.tmp
.vercel
coverage/
# Only ignore the local/machine-specific parts of .claude — settings.json
# and hooks/ are checked in so every clone inherits the project's Claude
# Code configuration.
.claude/settings.local.json
.claude/.session-start-sha
.claude/worktrees/
